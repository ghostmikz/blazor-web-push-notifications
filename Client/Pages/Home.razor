@page "/"
@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        // The 'true' parameter does a page-load redirect
        NavigationManager.NavigateTo("login");
    }
}